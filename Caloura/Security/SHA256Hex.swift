import CryptoKit
import Foundation

enum SHA256Hex {
    static func encode(_ input: String) -> String {
        SHA256.hash(data: Data(input.utf8))
            .map { String(format: "%02x", $0) }
            .joined()
    }
}
